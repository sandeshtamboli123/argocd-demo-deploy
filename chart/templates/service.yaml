apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.label }}
  labels:
    app: {{ .Values.label }}
spec:
  ports:
    - port: {{ .Values.port }}
      targetPort: 5000
  selector:
    app: {{ .Values.label }}
    tier: {{ .Values.label }}
  type: {{ .Values.service.type }}
